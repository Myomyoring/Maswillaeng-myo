import styled from 'styled-components';
import tw from 'twin.macro';

export const BoardPageStyle = styled.div`
  ${tw`
        w-full h-full
        desktop:p-16
        tablet:p-10
        mobile:p-4
    `}
`;
